name: 'air_quality_dbt'
version: '1.0.0'
config-version: 2

profile: 'air_quality_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  air_quality_dbt:
    staging:
      materialized: view
    intermediate:
      materialized: view
    marts:
      air_quality:
        materialized: table
      analytics:
        materialized: table